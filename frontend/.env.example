# Leave empty (or unset) for local dev — Vite will proxy /api to localhost:8000.
# Set this to your deployed backend URL for production builds:
# VITE_API_URL=https://devlens-api.onrender.com
VITE_API_URL=
